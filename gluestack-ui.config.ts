// gluestack-ui.config.ts
import { config as defaultConfig } from '@gluestack-ui/config';

// Export the default theme config so all components (Button, Input, etc.) render properly
export const config = defaultConfig;
